import { InfoquanlificationEntity } from './infoquanlification.entity';

describe('InfoquanlificationEntity', () => {
  it('should be defined', () => {
    expect(new InfoquanlificationEntity()).toBeDefined();
  });
});
